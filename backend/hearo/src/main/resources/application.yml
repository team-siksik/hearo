spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hearo
    username: hearo
    password: 'hearo'
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # ??? ?? ??
    show-sql: true      # SQL ?? ??
    properties:
      hibernate:
        format_sql: true  # SQL pretty print
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher